{% extends 'base.html.twig' %}

{% block title %}Créer une recette{% endblock %}

{% block body %}
<div class="container mt-5">

    <h1 class="mb-4 text-center">Créer une recette</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
</div>
{% endblock %}