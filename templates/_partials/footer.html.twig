<footer id="site-footer" class="mt-5">
    <div class="container py-4">
        <div class="row justify-content-center align-items-center g-4"> <!-- Centrage des éléments -->
            <!-- Logo et description (à gauche) -->
            <div class="col-md-4 mb-3 text-center"> <!-- Centré dans la colonne -->
                <div class="d-flex justify-content-center align-items-center">
                    <img src="{{ asset('images/logo.png') }}" alt="Logo" width="40" height="40" class="me-2">
                    <h5 class="mb-0">Marmiton</h5>
                </div>
                <p>Partagez, créez et découvrez les meilleures recettes maison.</p>
            </div>

            <!-- Liens de navigation (au centre) -->
            <div class="col-md-4 text-center mb-3">
                <h5 class="footer-title">Navigation</h5>
                <ul class="list-unstyled">
                    <li><a href="{{ path('home_index') }}" class="footer-link text-decoration-none">Accueil</a></li>
                    <li><a href="{{ path('recette_create') }}" class="footer-link text-decoration-none">Nouvelle recette</a></li>
                    {% if app.user %}
                    <li><a href="{{ path('user_profile') }}" class="footer-link text-decoration-none">Mon profil</a></li>
                    <li><a href="{{ path('app_logout') }}" class="footer-link text-decoration-none">Déconnexion</a></li>
                    {% else %}
                    <li><a href="{{ path('app_login') }}" class="footer-link text-decoration-none">Connexion</a></li>
                    <li><a href="{{ path('app_register') }}" class="footer-link text-decoration-none">Inscription</a></li>
                    {% endif %}
                </ul>
            </div>

            <!-- Contact & réseaux sociaux (à droite) -->
            <div class="col-md-4 text-center mb-3"> <!-- Centré dans la colonne -->
                <h5 class="footer-title">Contact</h5>
                <p>Email : <a href="mailto:exemple@marmiton.com" class="footer-link">exemple@marmiton.com</a></p>

                <div class="mt-2">
                    <a href="https://github.com/fannysaez" class="footer-icon"><i class="fab fa-github fa-lg"></i></a>
                </div>
            </div>
        </div>
    </div>
</footer>
