<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <img src="{{ asset('images/logo.png') }}" alt="Logo" width="40" height="40" class="me-2">
        {# <a class="navbar-brand" href="{{ path('home_index') }}">Marmiton</a> #}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <!-- Menu gauche -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if app.user %}
                {% if 'ROLE_ADMIN' in app.user.roles %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_index') }}">Admin</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('recette_create') }}">Nouvelle Recette</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_ingredient') }}">Nouvelle Ingredient</a>
                </li>
                {% endif %}
            </ul>

            <!-- Menu droite -->
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-fill user-icon"></i> Bonjour, <span class="username">{{ app.user.username
                            }}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('user') }}">Mon profil</a></li>
                        <li><a class="dropdown-item" href="{{ path('user_favorites') }}">Favoris</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <a class="dropdown-item text-danger text-center" href="{{ path('app_logout') }}"
                                title="Déconnexion">
                                <i class="bi bi-box-arrow-right fs-5"></i>
                            </a>
                        </li>
                    </ul>
                </li>
                {% else %}
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-primary me-2" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-outline-success me-3" href="{{ path('app_register') }}">Inscription</a>
                </li>
                {% endif %}

                <!-- Switcher Dark/Light -->
                <li class="nav-item">
                    <div class="form-check form-switch text-dark">
                        <input class="form-check-input" type="checkbox" id="darkModeSwitch">
                        <label class="form-check-label" for="darkModeSwitch">🌙</label>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
<hr>